rtl-sdr
将你的基于 Realtek RTL2832 的 DVB 加密狗变成 SDR 接收器
======================================================================

更多信息请见：
https://osmocom.org/projects/rtl-sdr/wiki

**********************************************************************

*****************************
修改版 RTL-SDR 博客版本
*****************************

2023年8月25日：更新到最新的 Osmocom 上游版本，并添加了一些新功能，比如自动直接采样。同时增强了强制偏置T功能，使其在强制偏置T时无法关闭，即使调用了偏置T功能。移除了 rtl_tcp 的环形缓冲区修改，因为这似乎带来的麻烦比好处多。

1) VCO PLL 电流修复 - 提高了约1.5 GHz以上频率下的稳定性 https://www.rtl-sdr.com/beta-testing-a-modified-rtl-sdr-driver-for-l-band-heat-issues/
2) 启用 rtl_tcp 的直接采样
3) 通过设置未使用的 IR 来强制偏置T始终开启的破解方法在EEPROM中将端点位设置为0。例如，要强制BT始终开启，可以使用“rtl_eeprom -b 1”，要取消强制BT，则使用“rtl_eeprom -b 0”。

4) 重新利用“偏移调谐”功能来切换偏置T开/关。现在如果GUI中没有专门的按钮，我们可以在SDR#和其他程序中使用“偏移调谐”按钮来切换偏置T。

5) 添加了对基于R828D RTL-SDR Blog V4加密狗的支持。

6) 自动直接采样。对于像RTL-SDR Blog V3这样的R820T/R860加密狗，当频率低于24 MHz时，代码会自动切换到直接采样模式；当频率高于24 MHz时，会自动切换回正常采样模式。对于这些加密狗，无需再手动更改采样模式。

偏置T注意事项：务必确保在设备连接到短路天线时不要开启偏置T，除非线路中有LNA。不过，如果你不小心开启了，也不用担心，因为电路配有自复位热熔丝进行双重保护。
LDO 上有内置保护。只是尽量不要连续几天短路它，否则最终可能会损坏热熔断器。注意，hack 3) 只有在你的系统使用这个驱动时才有效。如果你的系统或软件使用的是另一个驱动分支，那么 EEPROM 信息将无法读取。所以一定要先完全清理系统中的旧驱动（参考下面的信息），并确保在编译后运行 sudo make install。在 Windows 上，确保你使用的是这个代码的 rtlsdr.dll 文件。 
******************** 安装（Linux）： 
******************** ***注意*** 
如果你之前通过包管理器安装过 librtlsdr-dev，应该在安装这些驱动前先移除它。要完全移除这些驱动，
可使用以下命令： sudo apt purge ^librtlsdr sudo rm -rvf /usr/lib/librtlsdr* /usr/include/rtl-sdr* /usr/local/lib/librtlsdr* /usr/local/include/rtl-sdr* /usr/local/include/rtl_* /usr/local/bin/rtl_
* ***现在安装驱动程序*** 
```bash
sudo apt update
sudo apt install libusb-1.0-0-dev git cmake pkg-config
git clone https://github.com/rtlsdrblog/rtl-sdr-blog
cd rtl-sdr-blog/
mkdir build
cd build
cmake ../ -DINSTALL_UDEV_RULES=ON
make
sudo make install
sudo cp ../rtl-sdr.rules /etc/udev/rules.d/
sudo ldconfig
echo 'blacklist dvb_usb_rtl28xxu' | sudo tee --append /etc/modprobe.d/blacklist-dvb_usb_rtl28xxu.conf
```
*********************** 替代的 Debian 软件包安装方法: ***********************
如果你的系统依赖于 Debian 软件包（例如 FlightRadar24、FlightAware、ADSBExchange 镜像），你可以通过这种方法直接更新它们:
```bash
sudo apt update
sudo apt install libusb-1.0-0-dev git cmake
sudo apt install debhelper
git clone https://github.com/rtlsdrblog/rtl-sdr-blog
cd rtl-sdr-blog
sudo dpkg-buildpackage -b --no-sign
cd ..
sudo dpkg -i librtlsdr0_*.deb
sudo dpkg -i librtlsdr-dev_*.deb
sudo dpkg -i rtl-sdr_*.deb
```
********************** 安装 (MacOS): **********************
首先确保你已经安装了 Homebrew 和 Xcode。打开终端并运行：

```
brew uninstall rtl-sdr
brew install cmake
brew install libusb
brew install pkgconfig
git clone https://github.com/rtlsdrblog/rtl-sdr-blog
cd rtl-sdr-blog
mkdir build
cd build/
cmake ../
make LIBRARY_PATH=/usr/local/lib
sudo make install
```

*********************** 安装（Windows）: ***********************

从 Releases 页面下载 Release.zip 文件。
对于 SDR#，从 x86 文件夹中提取 rtlsdr.dll 文件到 SDR# 文件夹。对于大多数其他 x64 程序，使用 x64 文件夹中的 rtlsdr.dll 文件。
